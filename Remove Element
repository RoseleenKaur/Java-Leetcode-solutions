class Solution {
    public int removeElement(int[] nums, int val) {
        int inposition=0;
        for(int i=0;i<nums.length;i++)
        {
            if(nums[i]!=val)
            {
                nums[inposition++]=nums[i];
            }
        }
        return inposition;
        
    }
}
